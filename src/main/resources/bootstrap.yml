server:
  port: '8888'
spring:
  cloud:
    config:
      server:
        git:
#          uri: file://${user.home}/config-repo
          uri:  file:///E:/github/configuration

######### For application properties loaded from local system - WITHOUT GIT
#spring:
#  profiles:
#    active:
#    - native
#  cloud:
#    config:
#      server:
#        native:
#          search-locations:
#          - file:///E:/github
